{
  "name": "omnix",
  "version": "1.0.0",
  "description": "An autonomous agent that lives on GitHub. No server. No owner. Only purpose.",
  "main": "agent/run.js",
  "type": "module",
  "scripts": {
    "cycle": "node agent/run.js",
    "test": "node agent/run.js --dry-run"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.36.0",
    "node-fetch": "^3.3.2"
  }
}
